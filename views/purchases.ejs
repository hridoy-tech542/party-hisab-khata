<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <title>সব ক্রয় বিল</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f8;
      padding: 20px;
    }

    h2 {
      text-align: center;
      color: #2c3e50;
    }

    a {
      text-decoration: none;
      color: white;
      padding: 5px 10px;
      border-radius: 4px;
      margin-right: 5px;
    }

    .btn-add { background: #007bff; }
    .btn-edit { background: #28a745; }
    .btn-delete { background: #c0392b; }

    table {
      width: 100%;
      border-collapse: collapse;
      background: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    th, td {
      padding: 10px;
      border: 1px solid #ddd;
      text-align: center;
    }

    th {
      background: #f0f0f0;
    }

    .back {
      display: inline-block;
      margin-top: 15px;
      text-decoration: none;
      color: #555;
    }
  </style>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>

<h2>📄 সব ক্রয় বিল</h2>

<a href="/purchases/add" class="btn-add">➕ নতুন ক্রয় যোগ করুন</a>
<br><br>

<table>
  <tr>
    <th>তারিখ</th>
    <th>মেমো নং</th>
    <th>সাপ্লায়ার</th>
    <th>টাকা</th>
    <th>Action</th>
  </tr>

  <% purchases.forEach(p => { %>
    <tr>
      <td><%= p.date ? p.date.toISOString().slice(0,10) : "-" %></td>
      <td><%= p.memoNo %></td>
      <td><%= p.supplier ? p.supplier.name : "-" %></td>
      <td><%= p.totalAmount %> ৳</td>
      <td>
        <a href="/purchases/edit/<%= p._id %>" class="btn-edit">✏️ Edit</a>
        <a href="/purchases/delete/<%= p._id %>" class="btn-delete" onclick="return confirm('এই বিল ডিলিট করবেন?')">🗑 Delete</a>
      </td>
    </tr>
  <% }) %>
</table>

<br>
<a href="/" class="back">← ড্যাশবোর্ড</a>

</body>
</html>

