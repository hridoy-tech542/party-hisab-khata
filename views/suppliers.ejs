<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <title>সাপ্লায়ার তালিকা</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f8;
      margin: 0;
      padding: 20px;
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
    }

    .btn-add {
      display: inline-block;
      margin-bottom: 15px;
      padding: 10px 15px;
      background: #007bff;
      color: white;
      border-radius: 5px;
      text-decoration: none;
      font-weight: bold;
    }

    .btn-add:hover {
      background: #0056b3;
    }

    /* Table styles */
    .table-container {
      overflow-x: auto;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      min-width: 500px;
    }

    th, td {
      padding: 12px;
      border: 1px solid #ddd;
      text-align: center;
    }

    th {
      background: #f0f0f0;
    }

    a.btn-edit {
      background: #28a745;
      padding: 5px 10px;
      border-radius: 4px;
      color: white;
      text-decoration: none;
      margin-right: 5px;
    }

    a.btn-edit:hover {
      background: #218838;
    }

    a.btn-delete {
      background: #c0392b;
      padding: 5px 10px;
      border-radius: 4px;
      color: white;
      text-decoration: none;
    }

    a.btn-delete:hover {
      background: #922b21;
    }

    .back{
      display:block;
      margin-top:12px;
      text-align:center;
      text-decoration:none;
      color:#5989d6;
      border: 2px solid black;
    }
    .btn-gray{
    display:inline-block;
    margin-bottom:15px;
    padding:10px 15px;
    background:#2b354c;
    color:white;
    text-decoration:none;
    border-radius:8px;
    }

    /* Card view for mobile */
    @media (max-width: 768px) {
      table, thead, tbody, th, td, tr {
        display: block;
      }

      thead {
        display: none;
      }

      tr {
        background: white;
        margin-bottom: 15px;
        border-radius: 8px;
        padding: 15px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      }

      td {
        text-align: right;
        padding-left: 50%;
        position: relative;
      }

      td::before {
        content: attr(data-label);
        position: absolute;
        left: 15px;
        width: 45%;
        font-weight: bold;
        text-align: left;
      }

      a.btn-edit, a.btn-delete {
        display: inline-block;
        margin-top: 10px;
      }
    }
  </style>
</head>
<body>

<h2>👥 সাপ্লায়ার তালিকা</h2>

<a href="/suppliers/add" class="btn-add">➕ নতুন সাপ্লায়ার</a>
<a href="/" class="btn-gray">← ড্যাশবোর্ড</a>

<div class="table-container">
  <table>
    <thead>
      <tr>
        <th>নাম</th>
        <th>মোবাইল</th>
        <th>ঠিকানা</th>
        <th>শুরুর বকেয়া</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% suppliers.forEach(s => { %>
        <tr>
          <td data-label="নাম"><%= s.name %></td>
          <td data-label="মোবাইল"><%= s.mobile || "-" %></td>
          <td data-label="ঠিকানা"><%= s.address || "-" %></td>
          <td data-label="শুরুর বকেয়া"><%= s.openingBalance || 0 %> ৳</td>
          <td data-label="Action">
            <a href="/suppliers/edit/<%= s._id %>" class="btn-edit">✏️ Edit</a>
            <a href="/suppliers/delete/<%= s._id %>" class="btn-delete" onclick="return confirm('ডিলিট করতে চান?')">🗑 Delete</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

</body>
</html>
